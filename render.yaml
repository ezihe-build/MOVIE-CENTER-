services:
  - type: web
    name: ezihe-movie-box
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
